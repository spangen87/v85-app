---
phase: 03-track-cs-adjustment-admin-ui
plan: 05
type: execute
wave: 3
gap_closure: true
gap_source: 03-UAT.md
gap_test: 5
depends_on:
  - "03-03-PLAN.md"
files_modified:
  - components/admin/TrackConfigRow.tsx
autonomous: true
requirements:
  - TRACK-UI-01

must_haves:
  truths:
    - "Admin-sidan visar tydligt vad varje inställning gör"
  artifacts:
    - path: "components/admin/TrackConfigRow.tsx"
      provides: "Per-track-row component with help text under each label"
      contains: "hjälptext"
---

<objective>
Close UAT gap from test 5: add short help text under each field label in `TrackConfigRow.tsx` so the admin user understands what each setting controls and its CS effect.

UAT finding: "Det framgår inte riktigt vad de olika inställningarna gör. Behöver vara tydligare."

Root cause: Labels (Open stretch, Gynnade spår, Distansgräns) have no description of what they control or how they affect CS scores.
</objective>

<tasks>

## Task 1 — Add help text under each label in TrackConfigRow.tsx

File: `components/admin/TrackConfigRow.tsx`

Add a descriptive `<p>` help text under each label:

1. **Open stretch toggle** — after the label span, add a paragraph:
   "Hästar på gynnade spår får +0.12 CS. Aktiverar spårinställningen nedan."

2. **Gynnade spår (open_stretch_lanes)** — under the `<label>` element, add:
   "Ange spårnummer 1–20, kommaseparerade. Gäller bara när Open stretch är på."

3. **Distansgräns (short_race_threshold)** — under the `<label>` element, add:
   "Lopp kortare än detta värde sänker CS med 0.08. Sätt 0 för att inaktivera."

Style: `text-xs text-gray-500 dark:text-gray-400 mt-0.5` on each help paragraph.

</tasks>
